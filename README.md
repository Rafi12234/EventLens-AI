# EventLens-AI
EventLens AI is a local deep-learning-based image retrieval system that scans event decoration photos, creates AI embeddings with OpenCLIP, stores them in LanceDB, and allows natural-language search such as “white background”, “bride in red lehenga”, or “golden wedding stage” without using any cloud service.
